"use client"; import {useEffect,useState} from "react"; type I={service:string;key:string;configured:boolean}; export default function Home(){const [ints,setInts]=useState<I[]>([]),[mode,setMode]=useState("Inspect"),[msg,setMsg]=useState(""),[out,setOut]=useState("Готов. Inspect — проверка, Plan — безопасный план без изменений."),[busy,setBusy]=useState(false),[confirmed,setConfirmed]=useState(false); const refresh=()=>fetch("/api/integrations").then(r=>r.json()).then(j=>setInts(j.integrations||[])); useEffect(()=>{refresh()},[]); const run=async()=>{if(!msg.trim()||busy)return;setBusy(true);setOut("Агент анализирует задачу...");try{const r=await fetch("/api/task",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({mode,message:msg})});const j=await r.json(); if(j.fallback==="Offline"&&mode!=="Offline"){ const fr=await fetch("/api/task",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({mode:"Offline",message:msg})}); const fj=await fr.json(); setOut((j.output||"AI Gateway недоступен.")+"\n\n[Автоматически переключено на Offline]\n"+(fj.output||fj.error||"Offline не вернул результат.")); } else { setOut(j.output||j.error||"Нет ответа") }}catch{setOut("Ошибка соединения с Agent Hub.")}finally{setBusy(false);refresh()}}; const prepareDraft=async()=>{if(!msg.trim()||busy)return;setBusy(true);setConfirmed(true);setOut("Агент готовит Draft PR...");try{const r=await fetch("/api/task",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({mode:"Prepare",message:msg,confirmed:true})});const j=await r.json();setOut(j.output||j.error||"Нет ответа")}catch{setOut("Ошибка соединения с Agent Hub.")}finally{setBusy(false);refresh()}}; const status=(s:string)=>{const x=ints.find(i=>i.service.toLowerCase().includes(s)); return x?.configured?((s==="ai gateway")?"KEY CONFIGURED":"CONNECTED"):"NOT CONNECTED"}; return <main className="shell"><span className="badge">⚡ AGENT HUB · PRINTSHOP</span><h1 className="title">PRINTSHOP Engineer</h1><p className="muted">AI engineering manager для существующего PRINTSHOP.</p><section className="grid">{["github","vercel","supabase","ai gateway"].map(n=><div className="card" key={n}><div className="small muted">{n}</div><strong className={connected(n)?"ok":"bad"}>{status(n)}</strong></div>)}</section><section className="console"><div className="modes">{["Inspect","Plan","Offline","Fix","Improve","Deploy"].map(m=><button className={"mode "+(mode===m?"active":"")} onClick={()=>{setMode(m);setConfirmed(false)}} key={m}>{m}</button>)}</div><div className="row"><input className="input" value={msg} onChange={e=>setMsg(e.target.value)} onKeyDown={e=>e.key==="Enter"&&run()} placeholder="Например: проверь последний deployment PRINTSHOP"/><button className="send" onClick={run} disabled={busy}>{busy?"...":"Запустить"}</button></div><div className="output">{out}</div>{mode==="Plan"&&<button className="send" onClick={prepareDraft} disabled={confirmed||busy}> {confirmed?"Подготовка...":"Подтвердить план и подготовить Draft PR"}</button>}</section><div className="footer">Protected: production DB · auth · payments · orders. Конструктор PRINTSHOP не изменяется без явного запроса.</div></main>}